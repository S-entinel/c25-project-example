name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  speak:
    name: Say Hello
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "Langdale Zoo says hi! CI is alive and roaring."
